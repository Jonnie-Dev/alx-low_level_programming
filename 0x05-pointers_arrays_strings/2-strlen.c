#include "main.h"

/**
 * _strlen - returns the length of string
 * @s: string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int length = strlen(s);

	printf("%d\n", length);

	return (0);
}
